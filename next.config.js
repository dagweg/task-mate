/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ["avatars.githubusercontent.com", "wallpapershome.com", 'i.imgur.com']
    }
}

module.exports = nextConfig
